export const setFolder = (payload) => ({
	type: 'SET_FOLDER',
	payload,
});

export const addFolder = (payload) => ({
	type: 'ADD_FOLDER',
	payload,
});

export const updateFolder = (payload) => ({
	type: 'UPDATE_FOLDER',
	payload,
});

export const deleteFolder = (payload) => ({
	type: 'DELETE_FOLDER',
	payload,
});
